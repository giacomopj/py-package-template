flowchart TB
    subgraph Docker
    subgraph Volumes
    *app*-logs
    *app*-plots
    end
    subgraph Containers
    run-*app*
    debug-*app*
    test-*app*
    end
    Containers-->*app*-logs
    Containers-->*app*-plots
    end
    subgraph Host OS
    subgraph VS Code Project Directory .../*app*
    /bin
    /data<-->run-*app*
    /data<-->debug-*app*
    /data<-->test-*app*
    /docs
    /logs
    /plots
    /scrits
    /src-->run-*app*
    /src-->debug-*app*
    /src-->test-*app*
    /tests
    end
    end
